json.extract! detalle_ot_presupuesto, :id, :cantidad, :precio_venta, :subtotal, :repuesto_servicio_id, :orden_trabajo_presupuesto_id, :created_at, :updated_at
json.url detalle_ot_presupuesto_url(detalle_ot_presupuesto, format: :json)
